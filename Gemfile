source 'https://rubygems.org'

gem 'jekyll'
gem 'minima'

# Minima plugin already covers the jekyll-feed and jekyll-seo plugins so those are not included.
#  https://github.com/jekyll/minima/blob/master/minima.gemspec
group :jekyll_plugins do
  gem 'jekyll-sitemap'
end
